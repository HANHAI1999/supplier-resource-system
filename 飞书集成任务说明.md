# 联宇资源池 × 飞书多维表格集成 — 任务说明书（给 Codex）

## 一、背景与目标

**项目**：联宇资源池（供应商资源管理系统）
- 本地路径：`/Users/hanwenguo/Desktop/AI工作站/联宇资源池/`
- 技术栈：Vue 3 + Vite + Element Plus + ECharts + xlsx + sortablejs
- 现状：纯前端应用，数据存浏览器 localStorage（key: `supplier-resource-system-v1`），账号前端校验，无后端、无数据库
- 部署：GitHub Pages（gh-pages 分支），仓库 `git@github.com:HANHAI1999/supplier-resource-system.git`

**目标**：
1. 用**飞书多维表格**做数据后端：供应商、资源配置、联系人、异常记录集中存储，多人共享同一份数据
2. 基于表格做**供应商报名**：发给新供应商一个飞书表单链接，对方填写后数据自动进表，用于收集服务能力信息

## 二、飞书侧现状（重要）

- 多维表格 app_token：`J5TVb7bHLaI06PsgH93cA7Munxf`
- 现有数据表：`tblY2Qt1JTTbj6Sf`（名为「数据表」，空表）
- 已把应用「瀚海」添加为表格协作者（可编辑）
- **读取 OK**：`get_bitable_records` 已成功
- **写入 403（Forbidden）**：`add_bitable_record` 和建表 API 都返回 403
- 原因判断：应用缺**记录级**权限（截图显示已开通的都是 base:app:* 表格级权限，没有记录级）

## 三、要做的三件事

### 第 1 件：飞书应用补权限（用户侧或指导用户做）
在 `open.feishu.cn/app` → 应用「瀚海」→ 权限管理，开通以下**记录级**权限并发布版本：
- `bitable:record:create`（新增多维表格记录）
- `bitable:record:update`（更新多维表格记录）
- `bitable:record:delete`（删除多维表格记录）
- `bitable:record:read`（读取多维表格记录）
- 如无上述新命名，就搜 `bitable:app`（编辑多维表格）并开通

### 第 2 件：建表结构（5 张表，字段清单见第四节）
用飞书开放 API 在 `app_token = J5TVb7bHLaI06PsgH93cA7Munxf` 下创建 5 张表：
1. 供应商主表
2. 资源配置表
3. 联系人表
4. 异常记录表
5. 供应商报名表（给外部供应商填，之后在飞书里给它建「表单视图」生成链接）

### 第 3 件：网站改造
1. 数据层切换到飞书：读取 5 张表 → 映射成网站现有 suppliers 结构（含 cnConfigs / abilityConfigs / contacts / exceptions）
2. 写入：新增/编辑供应商、异常记录写回飞书
3. 推荐方案：加一个轻量后端（Vercel Serverless，Node fetch 调飞书 API）代理 tenant_access_token 与读写；不要把 app_secret 放前端
4. 供应商报名：飞书表建表单视图 → 链接发给供应商 → 数据自动进「供应商报名表」→ 管理员审核后入库

## 四、5 张表字段结构（创建表时用）

字段类型：1=多行文本，3=单选，4=多选，5=日期，17=附件

### 1. 供应商主表
| 字段名 | 类型 |
|---|---|
| 供应商名称 | 1 |
| 合作状态 | 3（使用中/储备/暂停合作/已淘汰） |
| 是否签署合同 | 3（是/否） |
| 供应商账期 | 1 |
| 对账人 | 1 |
| 供应商承接能力 | 1 |
| 公司介绍 | 1 |
| 资源盘归属 | 4（劲港资源池/美盈资源池） |
| 合作主体 | 1 |
| 内部对接人 | 1 |
| 备注 | 1 |
| 供应商评价 | 1 |

### 2. 资源配置表
| 字段名 | 类型 |
|---|---|
| 供应商名称 | 1 |
| 适用区域 | 1 |
| 业务类型 | 4（整柜/散货） |
| 服务场景 | 4（FBA/FBX） |
| FBA 备注 | 1 |
| FBX 备注 | 1 |
| 配置备注 | 1 |
| 美国资源池归属 | 4（美西/美东） |
| 服务能力 | 4（提柜/53尺派送 Solo/53尺派送 Team/26尺/Broker/LTL/倒货/拆柜合作） |

### 3. 联系人表
| 字段名 | 类型 |
|---|---|
| 供应商名称 | 1 |
| 供应商联系人 | 1 |
| 职务 | 1 |
| 联系方式 | 1 |
| 备注 | 1 |

### 4. 异常记录表
| 字段名 | 类型 |
|---|---|
| 供应商名称 | 1 |
| 异常时间 | 5（yyyy/MM/dd HH:mm） |
| 关联编号 | 1 |
| 涉及业务 | 4（整柜/散货） |
| 服务场景 | 4（FBA/FBX） |
| 涉及区域 | 1 |
| 处理状态 | 3（待处理/处理中/已解决/暂无法解决） |
| 异常说明 | 1 |
| 处理过程 | 1 |
| 结果说明 | 1 |
| 相关图片 | 17（附件） |

### 5. 供应商报名表（供应商自助填写）
| 字段名 | 类型 |
|---|---|
| 公司名称 | 1 |
| 联系人 | 1 |
| 职务 | 1 |
| 联系方式 | 1 |
| 国家/区域 | 1 |
| 可服务区域 | 1 |
| 业务类型 | 4（整柜/散货） |
| 服务场景 | 4（FBA/FBX） |
| 自有车队 | 3（是/否） |
| 车队规模 | 1 |
| 自有仓库 | 3（是/否） |
| 仓库信息 | 1 |
| 服务能力简介 | 1 |
| 合作意向备注 | 1 |

## 五、可用资源与提示

- 飞书 MCP：`feishu-full-mcp`（脚本 `/Users/hanwenguo/lark-tools-mcp/feishu_full_mcp.py`），已连接、可读（get_wiki_node / get_bitable_records / list_bitable_tables / search_users）
- 飞书应用凭证：在 `~/.reasonix/config.toml` 中 `[mcp-servers.feishu-mcp-server]` 段的 `--app_id` 与 `--app_secret`；另有 `~/.reasonix/.env` 存 `FEISHU_APP_ID/FEISHU_APP_SECRET`
- 飞书 API 基础：
  - 取 token：`POST https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal`（app_id+app_secret）
  - 建表：`POST https://open.feishu.cn/open-apis/bitable/v1/apps/{app_token}/tables`
  - 读写记录：`GET/POST https://open.feishu.cn/open-apis/bitable/v1/apps/{app_token}/tables/{table_id}/records`
- 建字段 API：`POST .../tables/{table_id}/fields`

## 六、验收标准

1. 飞书里能看到 5 张表且字段齐全
2. 能通过 API 往「供应商主表」写入一条测试记录并读回
3. 网站能从飞书读取供应商并展示（至少主表数据）
4. 「供应商报名表」能生成表单视图链接，外部人可填写
